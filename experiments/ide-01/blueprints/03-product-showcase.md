# Blueprint: product-showcase
**File:** `src/components/sections/ProductShowcase.tsx`

Purpose: Show the harness as a command center for coordinating parallel coding agents across workspaces while keeping every change inspectable before merge.

Slots: eyebrow · headline · body · cta_label

Assets:
- One product capture showing multiple coding agents running concurrently against separate repository workspaces.
- One product capture showing Projects or shared-plan organization across the active agent conversations.
- One product capture showing the harness coordinating agent status and workspace progress from a central command view.
- One product capture showing a completed agent change opened as a reviewable diff before it lands.

Structure: Use a product-level headline and concise supporting body to establish the command-center framing, followed by one focused CTA labelled for starting the harness. Support the copy with four product captures covering parallel agents, workspace or project organization, central coordination, and explicit diff review, rather than a feature list. Keep the four visuals tied to the same operating workflow and do not add testimonial, comparison, or repeated feature-card content already handled by neighbouring sections. On mobile, stack the heading and CTA before the product captures, then reflow the four visuals into a single vertical sequence; do not use numbered markers because this section presents coordinated capabilities rather than a prescribed user sequence.
